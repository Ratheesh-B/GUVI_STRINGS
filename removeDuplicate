s=input()
org=[]
dup=[]
res=[]
for i in range(0,len(s)):
    if s[i] not in org:
        org.append(s[i])
    elif s[i] not in dup:
        dup.append(s[i])
for j in org:
    if j not in dup:
        print(j,end="")
